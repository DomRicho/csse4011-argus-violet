# Advanced logging configuration
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y  # This ensures logs are output immediately
CONFIG_LOG_BACKEND_UART=y    # Use UART backend
CONFIG_UART_CONSOLE=y        # Enable UART console
CONFIG_PRINTK=y              # Enable printk